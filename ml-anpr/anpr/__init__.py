"""ANPR pipeline implementation. See detect_plate.py for the stable,
flat entry point (`import detect_plate; detect_plate.X`) that consumers
of this repo already depend on -- import from here directly only when
writing new code inside this package."""
