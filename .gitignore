# Node
node_modules/
.next/
dist/
build/
*.tsbuildinfo

# Python
__pycache__/
*.pyc
venv/
.venv/
.venv-ocr/

# Env
.env
.env.local
streaming/.organizer-password
streaming/snmp/targets.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
# MediaMTX & Streaming binaries/temp files
streaming/mediamtx
streaming/mediamtx.yml
streaming/videos/
*.mp4
docker-compose.override.yml
*.pt
# Fine-tuned model this project actually produced and ships with --
# unlike the blanket *.pt rule above (pretrained base weights teammates
# can just re-download), this one has no other source, so it needs to
# be tracked for the plate-presence speed gate to work for anyone else.
!ml-anpr/plate_presence_classifier.pt
debug_*.jpg

# Local dev tunnel script (machine-specific)
scripts/start-tunnels.sh

# ml-anpr demo run output (local test artifacts, not source)
ml-anpr/logs/
ml-anpr/mlruns/
ml-anpr/test_images/before/
ml-anpr/test_images/vehicle_boxes/
ml-anpr/test_images/plate_boxes/
ml-anpr/test_images/after/

# Local cloudflared tunnel logs (machine-specific)
cf-tunnel-*.log

# Local planning/working docs (never pushed)
docs/
