# Frontend Dashboard

Live viewing + alerts UI. Plays department feeds via hls.js against MediaMTX's HLS output (`streaming/`), and surfaces real-time alerts when `ml-anpr/` + `backend-watchlist/` detect a watchlist match.
