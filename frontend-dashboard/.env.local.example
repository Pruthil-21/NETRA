NEXT_PUBLIC_REGISTRY_API_URL=http://localhost:8000
NEXT_PUBLIC_WATCHLIST_API_URL=http://localhost:8001
NEXT_PUBLIC_MEDIAMTX_HLS_URL=http://localhost:8888

# HACKATHON SHORTCUT: a pre-minted JWT with role "officer", signed with backend-watchlist's
# JWT_SECRET. backend-watchlist has no login endpoint yet, so there's no way to obtain this
# from the app itself — ask whoever owns backend-watchlist to mint one for the demo.
NEXT_PUBLIC_DEMO_OFFICER_JWT=your_demo_jwt_here
