# Frontend Map

Next.js + Leaflet app rendering the camera registry as an interactive GIS map — pins by department, camera type, and connectivity/health status, with filters.

Consumes `backend-registry/`'s API (built initially against a `json-server` mock matching `/contract/API_CONTRACT.md`, then swapped to the real API).
