FROM alpine:3.22

RUN apk add --no-cache \
    bash \
    ffmpeg

COPY docker/demo-replay-entrypoint.sh /usr/local/bin/demo-replay-entrypoint

RUN chmod +x /usr/local/bin/demo-replay-entrypoint

ENTRYPOINT ["/usr/local/bin/demo-replay-entrypoint"]
