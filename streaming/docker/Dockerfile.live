FROM alpine:3.22

RUN apk add --no-cache \
    bash \
    curl \
    ffmpeg \
    jq

COPY docker/live-entrypoint.sh /usr/local/bin/live-entrypoint

RUN chmod +x /usr/local/bin/live-entrypoint

ENTRYPOINT ["/usr/local/bin/live-entrypoint"]
